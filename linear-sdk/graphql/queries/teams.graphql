query ListTeams($first: Int!) {
  teams(first: $first) {
    nodes {
      id
      key
      name
      description
      members {
        nodes {
          id
          name
          displayName
          email
          active
        }
      }
    }
  }
}
